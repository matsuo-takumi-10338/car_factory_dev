bundle:
  name: cf_bundle

include:
  - infrastructure/*.yml

targets:
  dev:
    default: true
    workspace:
      host: https://dbc-599d6b62-77ee.cloud.databricks.com
    # mode: production
    # mode: development
  
  prod:
    workspace:
      host: 